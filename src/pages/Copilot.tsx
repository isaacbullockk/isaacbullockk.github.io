import PageStub from '@/components/PageStub';

export default function Copilot() {
  return (
    <PageStub
      number="07"
      eyebrow="THE CO-PILOT"
      title="The engine room is"
      accent="warming up."
      note="Paste a conversation, get the stage, the interest meter, the signals, and three research-tagged replies. Rules mode runs locally; AI mode uses your own API key."
    />
  );
}
